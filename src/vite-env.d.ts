/// <reference types="vite/client" />

// Version constants are NOT globals — import them from src/generated/version.ts so the
// client bundle and the wrangler-bundled Worker resolve the same value.
